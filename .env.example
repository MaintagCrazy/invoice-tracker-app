# Invoice Tracker App - Environment Variables

# OpenRouter API Key for AI chat (required for AI features)
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Gmail OAuth credentials (base64 encoded, required for email sending)
GMAIL_TOKEN_B64=
GMAIL_CREDENTIALS_B64=

# Database URL (optional, defaults to SQLite for local dev)
# Railway provides this automatically for PostgreSQL
DATABASE_URL=sqlite:///./invoices.db

# Optional API key for endpoint authentication
API_KEY=

# CORS Origins (comma-separated, optional)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
