# FastAPI
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Google Sheets (database)
gspread>=6.0.0
google-auth>=2.27.0

# PDF Generation (ksef2 requires weasyprint>=63.0)
weasyprint>=63.0

# HTTP Client (ksef2 requires httpx>=0.28.1)
httpx[http2]>=0.28.1

# Google APIs (Gmail + Drive)
google-api-python-client>=2.116.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Pydantic (ksef2 requires >=2.12.5)
pydantic>=2.12.5

# Utils
python-dotenv>=1.0.0

# EFB 223 Generator
anthropic>=0.40.0
openpyxl>=3.1.0
fpdf2>=2.7.0
pikepdf>=8.0.0

# KSeF 2.0 (Polish National e-Invoice System)
# Pinned: the FA(3) model style (pydantic vs dataclass) and method names change
# between releases; an unpinned floor silently drifted prod to 0.18.1 and broke
# both the health check and XML serialization. Re-test KSeF before bumping.
ksef2==0.18.1
